//definre an array, assign the values and find the desired value

#include <iostream>
using namespace std;
int main()
{
    int n;
    cout<<"Enter the size of the array:";
    cin>>n;

    int arr[n];
    for (int i=0; i<n; i++){
        cout<<"Enter the values of your 1D array":
        cin>>arr[];
    }

    int find;
    cout<<"Enter the number you desire to find: ";
    cin>>find;

    int j=0;
    while(j<n){
        if (arr[j])
    }

    return 0;
}